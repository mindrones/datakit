/**
 * Types with a length property.
 *
 * @example ArrayLike, String, etc.
 *
 * @since 0.1.0
 */
export type HasLength = {length: number};

/**
 * Types with a size property
 *
 * @example Set, Map, etc.
 *
 * @since 0.1.0
 */
export type HasSize = {size: number};
